# Ruby / rbenv — not in active use.
# Uncomment to re-enable if Ruby development is needed.
#
# export OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES
# if (( $+commands[rbenv] ))
# then
#   eval "$(rbenv init -)"
# fi
